seeds:
  - name: employees
    description: fichier csv permettant de matcher les employées avec les customers
    columns:
      - name: employee_id
      - name: customer_id
        tests:
          - unique
          - not_null
           